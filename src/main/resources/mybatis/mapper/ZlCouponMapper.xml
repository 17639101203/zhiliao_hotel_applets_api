<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhiliao.hotel.mapper.ZlCouponMapper">

    <select id="findByRecID" resultType="com.zhiliao.hotel.controller.myOrder.vo.CouponUserVO">
        SELECT
          zcu.CouponID,
          zcu.UserID,
          zc.DiscountMoney
        FROM
          zl_couponuser zcu
          LEFT JOIN zl_coupon zc
            ON zcu.CouponID = zc.CouponID
        WHERE zcu.RecID = #{recID}
        LIMIT 1
   </select>

    <select id="findAll" resultType="com.zhiliao.hotel.model.ZlCoupon">
        SELECT
        zc.*
        FROM
        zl_coupon zc
        LEFT JOIN zl_couponuser zcu ON zc.CouponID = zcu.CouponID
        WHERE
        UserID IS NULL
        AND zc.CouponStatus = 1
        AND #{date} &gt; zc.FetchBeginDate
        AND #{date} &lt; zc.FetchEndDate
    </select>

    <update id="updateById">
        update zl_coupon set CouponCount = CouponCount - 1,UpdateDate = #{date} where CouponID = #{couponid}
    </update>
</mapper>