<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhiliao.hotel.mapper.ZlExpressMapper">

    <select id="getExpressList" resultType="com.zhiliao.hotel.model.ZlExpress">
        SELECT
          *
        FROM
          zl_express
        WHERE IsDelete = 0
        ORDER BY Sort ASC
    </select>

</mapper>